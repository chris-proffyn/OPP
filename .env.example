# Supabase (OPP — Proffyn)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=

# Web app (Vite) — publishable key only (safe for client)
# Get from: Supabase Dashboard → Project Settings → API (anon public / publishable key)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server/backend only — NEVER use in client code or VITE_* variables
SUPABASE_SECRET_KEY=

# Edge Functions (bulk-create-test-users, bulk-delete-test-users)
# When deployed, Supabase injects SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY automatically.
# For local dev (supabase functions serve): put SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY
# in supabase/functions/.env (see .gitignore). Same value as SUPABASE_SECRET_KEY in Dashboard.
# SUPABASE_SERVICE_ROLE_KEY=
# Optional: ENVIRONMENT=production to require confirm_test_users in bulk-create body.
# ENVIRONMENT=

# Optional: Resend for auth emails (when configured)
# RESEND_API_KEY=
