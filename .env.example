# Supabase (OPP — Proffyn)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=

# Web app (Vite) — publishable key only (safe for client)
# Get from: Supabase Dashboard → Project Settings → API (anon public / publishable key)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server/backend only — NEVER use in client code or VITE_* variables
SUPABASE_SECRET_KEY=

# Optional: Resend for auth emails (when configured)
# RESEND_API_KEY=
